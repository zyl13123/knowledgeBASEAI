# 2026-08-08 工作日志

## Phase 3（对话体验）完成 ✅

用户按我给的三份代码自己手写完成 Phase 3：
1. `lib/services/chat-service.ts` — 流式生成（AsyncGenerator）+ startChat 多轮历史 + systemInstruction 提示词
2. `app/api/chat/route.ts` — SSE 流式响应（text/event-stream），逐段推文本 + 来源 + [DONE]
3. `app/page.tsx` — 气泡式聊天界面 + 流式读取（reader.read）+ react-markdown 渲染 + 多轮 messages 数组

## 踩坑记录（重要）

- **gemini-1.5-flash 已彻底下线**（404），**gemini-2.5-flash 对新用户不可用**（404），必须用 **gemini-3.6-flash**（2026-08 时点）
- **gemini-3.6-flash 高峰期会 503**（高流量），需要等一会或换 gemini-3.5-flash / 加重试
- **SDK @google/generative-ai 0.24.1 的 startChat + sendMessageStream 不自动格式化 systemInstruction**，字符串直接透传给 API 导致 400。修复：手动包成 `{ role: 'system', parts: [{ text: ... }] }` 对象格式
- react-markdown 必须手动安装：`pnpm add react-markdown`

## 数据库现状（2026-08-08 查询）

- documents 表：20 行（含成功/失败记录），字段 id/title/file_type/file_size/file_path/chunk_count/status/created_at
- document_chunks 表：120 行，字段 id/document_id/content/position/embedding/token_count/created_at
- **conversations / messages 表不存在**（Phase 3 需求文档里有设计，但未建表，多轮对话目前是前端内存版）

## 待办

- Phase 3 剩余：对话持久化（conversations/messages 表）可选做
- Phase 4：知识库管理页（删除按钮）、登录认证（NextAuth）、UI 美化、部署 Vercel、README
- 之前聊过的 P0 加固：鉴权/限流（API 裸奔）、结构化日志、超时控制

## 检索智能升级（方向①+②）设计定稿 ✅

用户选定"方向① 检索质量升级 + 方向② Agentic 多跳"，已产出两份设计文档：
- `docs/requirements-retrieval-upgrade.md`：FR1 混合检索 / FR2 查询改写 / FR3 粗召回+rerank / FR4 智能分块 / FR5 Agentic 多跳（规划模式+ReAct 模式），P0-P2 分级
- `docs/architecture-retrieval-upgrade.md`：5 环节流水线（router→rewriter→hybrid→reranker→chat-service），关键决策：
  - 关键词通道 = **LLM 提取关键词 + SQL ILIKE 匹配**（不用 tsvector，中文切不开；不用 pg_search，托管环境中文分词复杂）
  - 融合 = **RRF**（1/(60+rank)，只看排名不看分数）
  - rerank = **LLM 一次调用**（gemini-3.6-flash 当 reranker，20 候选→3 块，prompt 强调"信息互补"）
  - 多跳 = **规划模式先行**（LLM 拆子问题→并行检索→合并），ReAct 循环（function calling）为阶段 F 可选
  - 新增 `/api/chat/debug` 调试接口 + heading_path 列 + 重处理接口
- 实施顺序 A~F：混合检索→改写→分块→rerank→规划→ReAct，每阶段 CONFIG 开关可回滚
