# 2026-08-10 工作日志

## 阶段 B：查询改写（Query Rewrite）完成 ✅

### 新建文件
- `lib/services/query-rewriter.ts`：LLM 查询改写 + 关键词提取 + JSON 解析 + 降级兜底
  - 无历史时跳过 LLM 调用，直接走标点切词（省一次 API 调用）
  - 有历史时调 gemini-3.6-flash，输出 {standalone_question, keywords}
  - JSON 解析失败/LLM 报错 → 降级为原问题 + extractKeywordsFallback

### 修改文件
- `lib/config/constants.ts`：加 REWRITE_HISTORY_TURNS: 4
- `app/api/chat/route.ts`：删掉 extractKeywords，接入 rewriteQuery；embedding 用 standalone_question，关键词用 LLM 提取的

### 当前流水线
```
问题 + 历史 → rewriteQuery（改写+关键词）→ hybridSearch（双通道+RRF）→ generateAnswerStream（流式生成）
```

### 阶段 A+B 代码审查清理
- route.ts 旧 extractKeywords 注释代码已清理

## 代码学习记录
用户逐行学习了以下概念：
- Map.values() + 展开运算符 [......] 的用途
- 对象展开 {...c, rrf_score} 的用途（复制属性+追加字段，不修改原对象）
- .sort((a,b) => b-a) 降序排序原理
- .slice(0, n) 截取数组
- .slice(-n) 从尾部取
- 三元表达式 ? : 
- 模板字符串 `${}`
- .join('\n') 用换行拼接数组
- result.response.text() 为什么是方法不是属性（SDK 返回对象）
- 正则 /pattern/i 的 / 定界符
- 正则 ^ 开头 $ 结尾、replace 去掉 markdown 代码块
- LLM 提取关键词 vs 标点切词的区别
```

## 移动端响应式适配完成 ✅

### 修改文件
- `app/page.tsx`：7 处响应式改动
  - 新增 `mobileMenuOpen` 状态
  - 顶栏加汉堡按钮（md:hidden）+ px-4 md:px-6
  - 左栏 aside 改为 fixed md:relative + translate-x 滑入/滑出抽屉
  - 新增遮罩层（md:hidden fixed inset-0 bg-black/30）
  - 聊天头部/消息区/输入区内边距全部改为 px-4 md:px-8 响应式

## 部署相关
- 项目已推送 GitHub（zyl13123/knowledgeBASEAI）→ Vercel 部署
- Gemini 模型问题：gemini-3.6-flash 免费 20次/天，gemini-2.5-flash 对新用户下线，当前切换到 gemini-3.1-flash-lite
- embedding-service.ts 的 outputDimensionality 参数需要 `as any` 类型断言绕过 SDK 类型定义滞后
- TypeScript 版本从 ^5 升级到 ^5.9.3 解决 Vercel 构建报错
